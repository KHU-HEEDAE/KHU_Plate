class WriteReviewScreenArguments {
  final int id;
  final String imgPath;
  final String name;
  final double rate;
  final int reviewCount;

  WriteReviewScreenArguments(this.id, this.imgPath, this.name, this.rate, this.reviewCount);
}